# AlmaLinux EL10 rpcbind still creates this account in an RPM scriptlet.
# Keep the image bootc-clean by declaring the same static account for systemd-sysusers.
u rpc 32 'Rpcbind Daemon' /var/lib/rpcbind -
